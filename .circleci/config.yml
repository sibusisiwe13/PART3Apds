# This config was automatically generated from your source code
# Stacks detected: deps:node:Customerpaymentsportal
version: 2.1
orbs:
  node: circleci/node@5
jobs:
  test-node:
    # Install node dependencies and run tests
    executor: node/default
    working_directory: ~/project/Customerpaymentsportal
    steps:
      - checkout:
          path: ~/project
      - node/install-packages:
          pkg-manager: npm
      - run:
          name: Run tests
          command: echo \"No test specified in package.json\"
          # Job for running API tests with Newman
  newman-tests:
    docker:
      # Use Node.js image with Newman preinstalled
      - image: postman/newman:latest
    steps:
      - checkout
      - run:
          name: "Run Newman Tests"
          command: |
            newman run tests/postman_collection.json --reporters cli

  # Job for running unit tests
  unit-tests:
    docker:
      - image: cimg/node:current
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run Unit Tests
          command: npm test
          # Orchestrate jobs using workflows
workflows:
  full-pipeline:
    jobs:
      - say-hello
      - newman-tests
      - unit-tests
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
workflows:
  build-and-test:
    jobs:
      - test-node
    # - deploy:
    #     requires:
    #       - test-node
